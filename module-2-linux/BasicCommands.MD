pwd # show current directory
ls # list files
ls -l # detailed list
cd folder # move to folder
cd .. # go back
cd /etc # go to exact path

---

touch file.txt
mkdir projects
rm file.txt
rm -r folder/

---

cp a.txt b.txt
mv a.txt folder/

---

chmod 755 script.sh
chown user:group file.txt

---

For checking running process
ps aux
top

---

For checking logs
ls /var/log
tail -f /var/log/syslog
tail -f /var/log/auth.log

---

Newtworking
ip a
ping 8.8.8.8
curl www.google.com

---

Install packages
sudo apt update
sudo apt install nginx
sudo apt install git

---
